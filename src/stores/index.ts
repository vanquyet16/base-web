/**
 * @module stores
 * Barrel export cho toàn bộ Zustand stores (client-side UI state).
 *
 * @example
 * import { useUiStore } from "@/stores";
 */

// useUiStore — Global UI state: sidebar open/close, theme, etc.
export { useUiStore } from "./useUiStore";
